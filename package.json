{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "your-build-command"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
